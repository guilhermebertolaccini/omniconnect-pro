/**
 * Tipos compartilhados com omniconnect-backend / microserviço BotFlow.
 * `import type` — sem custo em runtime no bundle Vite.
 */
export type {
  BotifyHandoffWebhookPayload,
  BotifyHandoffWebhookData,
  BotifyLeadSummary,
  BotifyHandoffEventType,
} from '@omniconnect/shared-types';
