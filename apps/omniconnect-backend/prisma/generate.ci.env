# Stub URL apenas para `prisma generate` quando DATABASE_URL não está definida (CI / monorepo).
# Produção/desenvolvimento local: use DATABASE_URL real (ver .env.example).
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/postgres?schema=public"
